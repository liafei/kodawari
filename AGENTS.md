# Project Instructions

`CLAUDE.md` is the canonical project instruction file for this repository.

Before answering any code-related question, planning implementation work,
reviewing code, or changing files, read `CLAUDE.md` first. If it is missing,
say so explicitly before proceeding.

Do not duplicate long-lived project rules here. Keep this file as the Codex
entry point and maintain durable rules in `CLAUDE.md`.

Code quality gates use the shared `code_redline.REDLINE` standard through the
vendored `code-redline` package.

