# kodawari

> *拘り* — the Japanese concept of obsessive attention to detail, the
> craftsman's refusal to accept anything less than what the work demands.

**kodawari** is an autopilot for autonomous software delivery. You give it a
PRD, it ships the feature: planner drafts the contract, reviewer audits the
plan, executor writes the code, and a strict gate refuses to silently pass
anything that wasn't actually verified.

The key word is *strict*. kodawari's design center is a **no-fake-run policy**:
every "verify" actually runs `pytest`, every "peer review" actually calls a
reviewer model, every "approval" is anchored in a real artifact. There is no
fallback path that smuggles `passed=true` past a gate when the underlying
check never ran.

## Quickstart

```bash
# 1. Install (Python 3.11+)
pip install -e .

# 2. Generate config (interactive — picks a preset, writes models.yaml + .env.example)
kodawari init-wizard

# 3. Sanity-check (no network calls; verifies project root, planning dir, env vars)
kodawari doctor preflight --feature my-feature --prd ./PRD.md

# 4. Ship
kodawari work-all \
  --feature my-feature \
  --prd ./PRD.md \
  --planner-route model \
  --executor-backend openai_tool_use \
  --max-cycles 3 \
  --max-wall-clock-seconds 1800 \
  --real-peer-review
```

## How it works

```
            PRD.md
              │
              ▼
   ┌──────────────────────┐
   │  prd-intake          │  extract source-of-truth, layers, constraints
   └──────────────────────┘
              │
              ▼
   ┌──────────────────────┐
   │  architecture-plan   │  archetype + surfaces + module boundaries
   └──────────────────────┘
              │
              ▼
   ┌──────────────────────┐
   │  init                │  scaffold + persist SCAFFOLD_MANIFEST
   └──────────────────────┘
              │
              ▼
   ┌──────────────────────┐
   │  task-plan           │  TASK_GRAPH.json (5-7 vertical-slice tasks)
   └──────────────────────┘
              │
              ▼
   ┌─────────── per-task ──────────────────────────────────────┐
   │  design → implement → verify → rules_gate → peer_review   │
   │                                          │                │
   │                                          ▼                │
   │                          fix_round (if reviewer blocks)   │
   └───────────────────────────────────────────────────────────┘
              │
              ▼
       release gate → ship
```

Three LLM roles, each with its own model and transport:

- **Planner** — drafts and revises plans; reads PRD context, prior reviewer
  findings, repo inventory.
- **Reviewer (plan + impl)** — audits plans and code; can block with
  must-fix items.
- **Executor** — writes code via a strict tool-use protocol (read, search,
  patch, verify). Cannot violate declared file scope.

Roles are wired in `.claude/workflow/models.yaml` (generated by `init-wizard`).
You can mix providers: Claude planner + GPT reviewer + custom-HTTP executor.

## Distinctive guarantees

- **No-fake-run**: every reviewer call, every verify command, every gate
  decision is anchored to a real artifact. Silently-passing fallback paths
  fail closed under `KODAWARI_REVIEW_ENABLED=1`.
- **Contract-first**: PRD → INTAKE → ARCHITECTURE_PLAN → TASK_GRAPH →
  TASK_CARD is a typed, schema-validated chain. Downstream consumers read
  these artifacts as the single source of truth.
- **Greenfield first-class**: scaffold + persisted manifest + planner-mode
  signal means an empty directory + PRD goes end-to-end without manual
  archetype hints.
- **Wall-clock watchdog**: `--max-wall-clock-seconds` writes
  `ABORT_REPORT.json` and exits 124 (POSIX timeout convention) — no
  forever-running loops.
- **Closure-tracing dependency skips**: when a task fails, downstream tasks
  report `blocked_by: [<failed-ancestor>]` instead of just the immediate
  unsatisfied dep — root cause is one click away.

## Status

**v0.1 — initial public release.** Validated on a complete greenfield run
(spec → FastAPI service + tests, 5/5 tasks, 6/6 verify pytest invocations,
6/6 peer reviews with one fix-loop self-heal). Production strict mode is
the recommended config for non-toy projects.

Known limits:
- PRD intake heuristic is conservative. Non-FastAPI shapes (CLI, lib, data
  pipeline) work but may produce a low-confidence intake; `kodawari init`
  with explicit `--archetype` is the workaround.
- Release gate stops at `AWAITING_DECISION` by design — explicit `kodawari
  decide` step required before shipping.

## Layout

```
src/kodawari/             # core package
  autopilot/              # engine, planning, execution, review
  cli/                    # workflowctl/kodawari CLI commands
  gate/                   # code-quality gate profiles
  schemas/                # JSON-Schema contracts for every artifact
tests/                    # 200+ tests
docs/                     # QUICKSTART, USER_GUIDE, OPERATOR_RUNBOOK,
                          # CAPABILITY_MAP, contracts/, architecture/
adapters/                 # claude-code, codex-cli integration plugins
scripts/                  # bootstrap + maintenance scripts
```

## Documentation

- [docs/QUICKSTART.md](docs/QUICKSTART.md) — first-run walkthrough (30s noop,
  10min Claude subscription, greenfield from empty dir)
- [docs/USER_GUIDE.md](docs/USER_GUIDE.md) — full operator manual
- [docs/OPERATOR_RUNBOOK.md](docs/OPERATOR_RUNBOOK.md) — error codes,
  troubleshooting
- [docs/CAPABILITY_MAP.md](docs/CAPABILITY_MAP.md) — capability × backend
  wiring matrix
- [docs/contracts/ENV_VAR_REFERENCE.md](docs/contracts/ENV_VAR_REFERENCE.md)
  — every env var, what it does, what it defaults to

## License

MIT — see [LICENSE](LICENSE).

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md).
