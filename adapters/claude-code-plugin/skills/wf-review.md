# /wf-review

Thin facade for canonical review entrypoint.

```bash
kodawari wf-review --project-root . --feature <feature>
```

Canonical equivalent:

```bash
kodawari review --project-root . --feature <feature>
```
