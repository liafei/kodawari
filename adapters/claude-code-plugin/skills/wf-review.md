# /wf-review

Thin facade for canonical review entrypoint.

```bash
workflowctl wf-review --project-root . --feature <feature>
```

Canonical equivalent:

```bash
workflowctl review --project-root . --feature <feature>
```
