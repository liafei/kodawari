# /wf-work-all

Thin facade for canonical work-all entrypoint.

```bash
kodawari wf-work-all --project-root . --feature <feature> --prd <path>
```

Canonical equivalent:

```bash
kodawari work all --project-root . --feature <feature> --prd <path>
```
