# /wf-work-all

Thin facade for canonical work-all entrypoint.

```bash
workflowctl wf-work-all --project-root . --feature <feature> --prd <path>
```

Canonical equivalent:

```bash
workflowctl work all --project-root . --feature <feature> --prd <path>
```
