# 一、平台现状、架构与兼容总览

`kodawari.autopilot.engine`（引擎装配层）
`kodawari.autopilot.local_adapter`（本地适配层）

## 5. 兼容与历史吸收

`workflow-claude` 吸收策略：历史源码只作为只读证据，新的可执行入口以
`kodawari` 主线为准。

兼容命令族包括：

- `kodawari compact`
- `kodawari quick-develop`

历史索引文件用于追溯吸收边界：

- `WORKFLOW_CLAUDE_SOURCE_INDEX.md`
- `WORKFLOW_CLAUDE_TEST_DAMAGE_INDEX.md`
- `WORKFLOW_CLAUDE_ABSORPTION_PRIORITY.md`

Standing proof 工作流：`.github/workflows/kodawari-standing-proof.yml`。

代码质量红线来源固定为 `code_redline.REDLINE` / `code-redline`。

