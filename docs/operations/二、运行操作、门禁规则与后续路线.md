# 二、运行操作、门禁规则与后续路线

代码质量红线来源固定为 `code_redline.REDLINE` / `code-redline`。

Repo-local 启动入口：

- `bootstrap_kodawari.ps1`
- `.\scripts\kodawari.ps1 gate --help`
- `.\scripts\kodawari.ps1 telemetry --help`
- `.\.workflow_runtime\local-env\.venv\Scripts\kodawari.exe gate --help`

Lane wrappers:

- `.\scripts\run_always_on_lane.ps1`
- `.\scripts\run_integration_lane.ps1`
- `.\scripts\run_always_on_lane.ps1 -ListOnly`
- `.\scripts\run_integration_lane.ps1 -FailIfSkipped`
- `.\scripts\run_always_on_lane_repeat.ps1`
- `.\scripts\run_integration_lane_repeat.ps1`

固定 lane 产物：

- `lane_stability_always-on.json`
- `lane_stability_integration.json`
- `lane_triage_always-on.json`
- `lane_triage_integration.json`
- `lane_triage_always-on.md`
- `lane_triage_integration.md`
- `lane_history_manifest.json`
- `lane_weekly_trend.json`
- `lane_weekly_trend.md`

CI workflow:

- `.github/workflows/kodawari-always-on.yml`
- `.github/workflows/kodawari-integration.yml`
- `.github/workflows/kodawari-standing-proof.yml`
- `actions/upload-artifact@v4`
- `GITHUB_STEP_SUMMARY`

Real review lane environment:

- `WORKFLOW_REVIEWER_API_KEY`
- `WORKFLOW_REVIEWER_BASE_URL`
- `tests/test_generic_runtime_real_review.py`
- `tests/test_generic_runtime_proof.py`

Repeat runs record `Repeat 1` and `summary_version` in the stability summary.

## WS-270 ~ WS-281

当前状态为 `Done`。

- `WS-281: 现有 fitness checks 纳入 ratchet` `Done`
- 当前结果：`60 passed`

